<p id="notice"><%= notice %></p>

<h1>My Calendars</h1> <br>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @calendars.each do |calendar| %>
      <tr>
        <a href="calendars/<%=calendar.id%>" class="button"><%= calendar.title %></a>
        <t>
        </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Calendar', new_calendar_path %>
